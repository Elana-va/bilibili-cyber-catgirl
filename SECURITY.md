# Security Policy

## 支持范围

当前维护分支为 `main`。项目仍处于研究与试运营阶段，不承诺适用于公网、多租户或无人值守生产环境。

## 报告安全问题

请不要在公开 Issue 中提交 B站 Cookie、扫码数据、DeepSeek API Key、访问令牌、用户评论数据库、运行日志或可用于绕过平台验证与风控的细节。

请通过仓库 **Security → Report a vulnerability** 私密报告。报告中可包含影响范围、脱敏复现步骤、预期行为和修复建议，但不要附带真实凭证。

## 凭证泄露处理

1. 立即在对应平台撤销或轮换凭证；
2. 开启 `CATGIRL_KILL_SWITCH=true` 并停止服务；
3. 删除本地 `data/secrets/` 中的旧密文；
4. 检查审计日志与平台账号活动；
5. 不要把“从 Git 历史删除字符串”当作凭证轮换的替代品。

## 安全部署边界

- 默认只绑定 `127.0.0.1`，不要把当前后台直接暴露到公网；
- 不要在共享 Windows 用户下保存 DPAPI 凭证；
- 不要提交 `data/`、`.env`、日志或备份；
- 开启写入前先备份数据库并核对目标账号；
- 不要移除运行模式、自动回复和写入授权三道独立闸门；
- 不要实现验证码绕过、客户端注入、群控、刷量或风控规避。

详细安全设计见 [docs/architecture.md](docs/architecture.md)。
